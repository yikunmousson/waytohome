# 把下面这行改成你自己的高德 Web 服务 Key，然后把文件名改成 .env
# Key 申请：https://lbs.amap.com → 控制台 → 应用管理 → 我的应用 → 添加 Key → 服务平台选「Web服务」
#
# 注意：.env 已经被 .gitignore 忽略，不会被提交。如果你用 GitHub Actions 跑，
# 请把 Key 存到仓库的 Settings → Secrets and variables → Actions → New repository secret，
# 名字叫 AMAP_KEY，不要在代码里写明文。

AMAP_KEY=PUT_YOUR_AMAP_WEB_SERVICE_KEY_HERE

# 可选：iPhone 安装 Bark 后复制专属地址。不要提交到仓库或公开分享。
# GitHub Actions 中保存为同名 Secret；留空则不推送。
BARK_URL=

# 可选：通知点击后打开的 GitHub Pages HTTPS 地址。
# GitHub Actions 中保存为同名 Repository variable。
DASHBOARD_URL=
